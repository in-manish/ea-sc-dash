/**
 * WhatsApp templates — Communication → WhatsApp tab.
 *
 * Entry: `ui/WhatsAppConfig.jsx` (also default export from this feature)
 * Service: `src/services/whatsappService.js`
 *
 * Status values (backend STATUS_CHOICES): pending | approved | rejected | paused | disabled
 */
export { default } from './ui/WhatsAppConfig';
